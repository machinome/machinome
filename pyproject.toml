[build-system]
requires = ["setuptools>=65.5,<77"]
build-backend = "setuptools.build_meta"

[project]
name = "machinome"
version = "0.7.0"
description = "Source code for machines — a Python framework for mechanical design and simulation"
readme = { file = "README.rst", content-type = "text/x-rst" }
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [
    { name = "Luis Fagundes", email = "lhfagundes@gmail.com" },
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Natural Language :: English",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
keywords = ["machinome"]
dependencies = [
    "watchdog",
    "trimesh==4.4.*",
    "solidpython2==2.1.*",
    # cadquery and build123d both bind the single OCP module through the
    # cadquery-ocp distribution, so their ranges must intersect. This pair
    # is the newest that resolves to one OCP (7.8): cadquery 2.8 moved to
    # cadquery-ocp >=7.9 while build123d 0.11 moved to cadquery-ocp-novtk,
    # a second distribution installing the same OCP package.
    "cadquery==2.7.*",
    "build123d==0.10.*",
    # build123d 0.10 leaves this transitive dependency unbounded; 0.3
    # imports OCP 8 collections absent from our shared OCP 7.8 runtime.
    "ocp-gordon>=0.2.2,<0.3",
    # The flexible leaf evaluates molejo shapes, and MolejoNode reaches for
    # molejo's OCCT evaluator at import, so `brep` is required rather than
    # optional. Pinned to the minor: a molejo minor carries the shape-spec
    # version it implements, and the documents this framework writes name
    # that version.
    "molejo[brep]==0.2.*",
    "httpx==0.27.*",
    "fastapi==0.111.*",
    "termcolor==2.4.*",
    "asgiref==3.8.*",
    "uvicorn==0.30.*",
    "numpy==2.2.*",
    "manifold3d",
    "rtree",
    "scipy",
]

[project.optional-dependencies]
dev = [
    "bump2version",
    "flake8>=7",
    "tox>=4",
    "coverage>=7",
    "Sphinx>=7",
    "twine>=5",
    "pytest>=8",
    "pillow>=10",
    "black>=25.1",
    "build>=1.2",
    # tests/test_docs_exports.py reads .readthedocs.yaml and the Actions
    # workflow. PyYAML arrives anyway as a transitive of trame; this is
    # the direct use declared rather than left to that accident.
    "pyyaml",
    "pytest-watch",
    "ipython",
    "ipdb",
]
docs = [
    "sphinx",
    "sphinx_rtd_theme",
]
# The browser viewer is a separate, AGPL-3.0-only package. The framework is
# complete for non-interactive use without it; interactive development needs
# this extra. OpenSCAD remains the default fixed-pose snapshot renderer.
viewer = [
    "machinome-viewer",
]
# The web snapshot renderer needs the viewer and its browser; Chromium itself
# is a second step: `playwright install chromium`.
web-snapshot = [
    "machinome-viewer[snapshot]",
]
mechanics = [
    "machinome-mechanics",
]
# Machinome Studio is still experimental and unpublished. Declaring its final
# extra name now does not make the dependency available from a package index.
studio = [
    "machinome-studio",
]

[project.urls]
Homepage = "https://github.com/machinome/machinome-framework"

[project.scripts]
machinome = "machinome.cli:manage"

[tool.setuptools.packages.find]
include = ["machinome", "machinome.*"]

[tool.setuptools]
include-package-data = true
zip-safe = false

[tool.pytest.ini_options]
testpaths = ["tests"]
