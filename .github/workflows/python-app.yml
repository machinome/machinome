name: Python application

on: [push]

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        id: setup-python
        with:
          python-version: "3.12"
          cache: "pip"

      - uses: actions/cache@v3
        id: cache-primes
        with:
          path: venv
          key: venv-${{ steps.setup-python.outputs.python-version }}-${{ hashFiles('requirements*.txt') }}

      - name: Install requirements
        if: steps.cache-primes.outputs.cache-hit != 'true'
        run: |
          python -m venv venv
          . venv/bin/activate
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements_dev.txt

      - name: Black
        continue-on-error: true
        run: |
          . venv/bin/activate
          black --check machinome tests

      - name: Flake8
        continue-on-error: true
        run: |
          . venv/bin/activate
          flake8 --max-line-length=89 machinome tests

  test:
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - uses: actions/checkout@v3

      - uses: awalsh128/cache-apt-pkgs-action@latest
        with:
          packages: openscad
          version: 1.0

      - name: Setup Python
        uses: actions/setup-python@v4
        id: setup-python
        with:
          python-version: "3.12"
          cache: "pip"

      - uses: actions/cache@v3
        id: cache-primes
        with:
          path: venv
          key: venv-${{ steps.setup-python.outputs.python-version }}-${{ hashFiles('requirements*.txt') }}

      - name: Install requirements
        if: steps.cache-primes.outputs.cache-hit != 'true'
        run: |
          python -m venv venv
          . venv/bin/activate
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements_dev.txt

      - name: Run tests
        run: |
          . venv/bin/activate
          coverage run --source machinome -m pytest
          coverage report -m

  browser-snapshot:
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - uses: actions/checkout@v3

      - uses: awalsh128/cache-apt-pkgs-action@latest
        with:
          packages: openscad
          version: 1.0

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: |
            requirements.txt
            requirements_dev.txt

      - name: Install framework and browser snapshot dependencies
        run: |
          pip install -r requirements.txt -r requirements_dev.txt
          pip install "machinome-viewer[snapshot] @ git+https://github.com/machinome/machinome-viewer.git"
          playwright install --with-deps chromium

      - name: Run real browser snapshot test
        env:
          MACHINOME_WEB_SNAPSHOT_E2E: "1"
        run: >-
          pytest
          tests/test_browser_renderer.py::BrowserSnapshotEndToEndTest

  docs:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive

      # Metamaquina2's leaves read the original OpenSCAD design through
      # solid2's import_scad, so its export needs the openscad binary.
      # Kept in step with .readthedocs.yaml, which installs it for the
      # same reason.
      - uses: awalsh128/cache-apt-pkgs-action@latest
        with:
          packages: openscad
          version: 1.0

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: |
            docs/requirements.txt
            requirements.txt

      # The example exports ship the browser viewer, which is the separate
      # machinome-viewer package. Until it is on PyPI it is installed from
      # its repository; its source distribution builds the widget itself,
      # so node is needed here for that build and nothing else.
      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install runtime, viewer and docs requirements
        run: |
          pip install -r requirements.txt -r docs/requirements.txt
          pip install -e .
          pip install "machinome-viewer @ git+https://github.com/machinome/machinome-viewer.git"
          pip install "machinome-mechanics @ git+https://github.com/machinome/machinome-mechanics.git"

      - name: Generate V8 example export
        working-directory: docs/examples/v8-engine
        run: >-
          PYTHONPATH="$GITHUB_WORKSPACE" python -c
          "from machinome.cli import manage; manage()"
          export -o docs/_exports/v8-engine --fps 30 --frames 360

      - name: Generate Metamaquina 2 example export
        working-directory: docs/examples/metamaquina2
        run: >-
          PYTHONPATH="$GITHUB_WORKSPACE" python -c
          "from machinome.cli import manage; manage()"
          export -o docs/_exports/metamaquina2 --fps 30 --frames 360

      - name: Generate Clock 01 example export
        working-directory: docs/examples/3dprintedclocks
        run: >-
          PYTHONPATH="$GITHUB_WORKSPACE" python -c
          "from machinome.cli import manage; manage()"
          export -o docs/_exports/clock01 wall_clock_01

      - name: Build docs (fail on warnings)
        run: python -m sphinx -b html -W docs docs/_build

  build:
    if: github.event_name == 'push' && contains(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    needs: [test]
    permissions:
      contents: write

    env:
      MACHINOME_VERSION: "${{ github.ref_name }}"

    steps:
      - uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        id: setup-python
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install packaging
        run: pip install packaging

      - name: Detect package version
        id: package_version
        run: |
          from os import getenv as env
          from packaging.version import parse
          machinome_version = str(parse(env("MACHINOME_VERSION")))
          print(f"version={machinome_version}", file=open(env("GITHUB_OUTPUT"), "a"), flush=True)
        shell: python

      - uses: actions/cache@v3
        id: cache-primes
        with:
          path: venv
          key: venv-${{ steps.setup-python.outputs.python-version }}-${{ hashFiles('requirements*.txt') }}

      - name: Install requirements
        if: steps.cache-primes.outputs.cache-hit != 'true'
        run: |
          python -m venv venv
          . venv/bin/activate
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements_dev.txt

      - name: Build python package
        run: |
          . venv/bin/activate
          make dist

      - name: Create release
        if: steps.package_version.outputs.version != null
        run: |
          gh release create "$MACHINOME_VERSION" ./dist/* \
            --title "${{ steps.package_version.outputs.version }}" \
            --repo="$GITHUB_REPOSITORY"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
