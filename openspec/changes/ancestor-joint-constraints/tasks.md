## 1. Ratified baseline and red evidence

- [ ] 1.1 Obtain explicit ratification of this proposal, design and spec deltas; validate and create the planning-only commit on verified base `0ce71cd` with a clean one-commit ancestry.
- [ ] 1.2 Record passing baseline range, joint, clocked, running and ADR-133 time-drive tests plus representative publication identities before changing implementation.
- [ ] 1.3 Add red-first nested declaration and mechanical-stop tests, including the existing flat positive control; capture the missing public declaration failure on this base.

## 2. Scoped declaration and posing

- [ ] 2.1 Record and validate additive descendant scalar-joint constraints in assembly class bodies; prove bad target, malformed range, self-read, scope, inheritance and duplicate-read refusals.
- [ ] 2.2 Resolve contributions per instance while preserving the original joint and every tree/placement identity; test two independent copies and incompatible child overrides.
- [ ] 2.3 Enforce added limits at completed untimed enumeration with original limits intact, scoped diagnostics, known-bound checks despite unknown reads, and existing ownership exemptions.

## 3. Compile and execute the effective limits

- [ ] 3.1 Compile each contribution independently, intersect sides using existing expression graphs, union qualified reads, and refuse empty numeric intersections or invalid hidden contributions.
- [ ] 3.2 Prove nested running stops, moving-read restraints, relief without backlog, failed-request atomicity and exact snapshot/replay, including composed limits from multiple ancestors.
- [ ] 3.3 Prove clocked stops through the same compiler and preserve supported-level refusals; prove one constrained ADR-133 time drive stops without stopping elapsed time or unrelated drives.
- [ ] 3.4 Prove unchanged old-model publication/identity and changed effective-limit identity; validate generated nested documents and replay through the installed viewer without a format or viewer-code change.

## 4. Curta empirical validation

- [ ] 4.1 In Curta's repository preserve the real wrong-order request as red evidence using its actual crank, retained shaft and complete bell, without changing assembly/control paths or upstream CAD.
- [ ] 4.2 Measure and document the complete-bell contact bracket for the relevant retained phase; validate the explicitly bounded diagnostic window with free/blocked checks in both geometry kernels.
- [ ] 4.3 Apply the new declaration to Curta's existing descendant joints in its diagnostic; prove stopped crank travel, held shaft history, relieving motion and snapshot/replay, and inspect a stopped-geometry image.
- [ ] 4.4 Compare the assembly/control tree and hosted pointer outcome against the baseline; run focused operating arithmetic/reversal regressions and record tested framework/project/viewer content plus the remaining mechanical limits in the author-review record.

## 5. Complete the framework cycle

- [ ] 5.1 Update public framework API/scenario docs and changelog; after green implementation extract the accepted ADR amending ADR-113, update its index and the architecture synthesis.
- [ ] 5.2 Run the relevant full regression suites and supported OpenSpec validation; record results, baseline comparisons, Curta acceptance and any explicitly unproved operating cases.
- [ ] 5.3 Sync the accepted spec deltas, verify synchronization, archive the completed change and create the single implementation/evidence commit; verify clean two-commit ancestry and report integration state.
