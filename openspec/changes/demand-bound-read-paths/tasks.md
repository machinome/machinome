## 1. Red contract and baseline

- [ ] 1.1 Add a real running fixture whose retained own-read law is consumed only by a Bound; prove its missing demand/path red against `acebc48`.
- [ ] 1.2 Pin frozen-Curta ordinary-turn, active-stop, 213-bank and ordered searched-level baseline evidence, plus existing Play fallback and error behavior.

## 2. Small framework change

- [ ] 2.1 Include compiled Bound read keys in the private propagation demand set once per Program, leaving existing path construction/search/replay code unchanged.
- [ ] 2.2 Prove the fixture's actual path is traced while Play/untraced reads still replay and inactive constraints introduce no new error.

## 3. Acceptance

- [ ] 3.1 Run focused and broad framework regressions, strict OpenSpec validation, and exact frozen-Curta 2,600-level/free-turn/stopped-withdrawal parity with material CPU gain.
- [ ] 3.2 Record evidence and ADR disposition, sync the accepted spec, archive the change and integrate only after the stated gates pass.
