## 1. Prove the current repeated-sample cost

- [ ] 1.1 Add red-first tests for repeated path samples without node-key hashing, current standing values after rebinding, failed-bind isolation, numeric bit identity, and eager error order.
- [ ] 1.2 Record a frozen Curta `7586002` active-stop baseline and exact 213-bank witness under the current framework base.

## 2. Compile only the path's immutable moving structure

- [ ] 2.1 Build local moving and standing child positions after a successful first bind; refresh standing numeric slots only after every complete successful bind.
- [ ] 2.2 Evaluate each later point into fresh moving slots in original node/operand order with existing operators and visit accounting.

## 3. Verify and complete

- [ ] 3.1 Run focused and broad source-timing, carry, stop, and expression tests plus strict OpenSpec validation.
- [ ] 3.2 Compare exact active-stop Curta bank and pinned CPU timing before/after on the same project graph; record program identity, source hashes, and honest measured gain or abandon the optimization.
- [ ] 3.3 Synchronize baseline spec, archive the change, and integrate locally only after clean two-commit validation and verification of any concurrent primary advances.
