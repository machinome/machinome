## 1. Prove the failure

- [ ] 1.1 `tests/test_sphinx_ext.py`: the emitted iframe carries
      `allowfullscreen`; run it red.

## 2. Implement

- [ ] 2.1 `machinome/sphinx.py`: add `allowfullscreen` to the iframe; run the
      sphinx tests green.

## 3. Record

- [ ] 3.1 Validate, sync the delta spec, archive, commit.
