# Curta path-local bind evidence — 22 September 2026

Framework baseline: `a500a99a02178f8d1454b059fe16307a3f97230d`; candidate: isolated `reuse-identical-path-binds` worktree based on it. The pilot's standing autonomous framework-fix mandate covers this proposal and local implementation, not a separate ratification of its technical specifics. The primary framework checkout's untracked `docs/examples/v8-engine/` and the originating project's user video/images and dirty docs/trial files were preserved. No project source or law was edited by this cycle.

The frozen project is `projects/Calculators/Curta-Type-I-3x/WTs/perf-758-reference` at project commit `7586002`, program identity `be125f4048c8be00ce4e73d646311d4a50ac75a59aa739f93bf2f38c992fa195`. The current production checkout was project commit `884b01e` during the second pair and retained program identity `3e1d505123b75832f6e1b4353d7e9d7f5c5856375bcb67454ca15b602f140c20`. The latter has geometry/doc changes since the first Curta cache cycle, but the pinned running graph identity is unchanged. Program identity, not an unverified filename assumption, identifies each numerical comparison.

The read-only probe is the previous accepted `openspec/changes/archive/2026-09-22-cache-standing-bound-bind/curta_probe.py`. Run it from the selected project checkout with `PYTHONPATH=<framework-worktree>:<workspace>/molejo/python:<project-checkout>`, workspace `.venv/bin/python`, `OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 SOLID_BUILD_DIR=_build_checks`, and `taskset -c 14`. The final argument is `first48`, `turn`, or `stop`. `first48` uses default viewer cadence `dt=1/240`, while the project-style complete turn and stop use `dt=.1`; no internal sample count or tolerance changes. CPU figures below are process CPU seconds excluding initialization. Other host activity was uncontrolled, so speed claims are bounded paired observations, not frame-rate guarantees.

| Project/scenario | Baseline CPU s | Candidate CPU s | Ordered Bound samples and exact parity |
| --- | ---: | ---: | --- |
| Frozen `7586002`, first 48 ticks | 31.1595 | 25.9061 | 6,240; 213-bank SHA `33df3f395f95bdb969c4df73f211f1370b6e3adc499c4131e6aab46eaaa12ce4`, ordered input/result SHA `2d8ec8c55486c3742646243b18c9cd54a173e208e5862768181d41f08e1633ec`; active, admitted 36° |
| Frozen, complete 360° | 24.141¹ | 18.139 | 2,600; bank `0051c71e25b2f735d5664c4a012b3f7f6c9e5b76bf0f7cdd74792e5ee1df81c3`, ordered `27bdeb6014bc111d9a1c006ca0a73ebbde4e20691b8cf12eb626c3b69d254bba`; completed 360° |
| Frozen, active withdrawal stop and restore/replay | 16.760¹ | 12.957 | 398; bank `07b64aab52ef78c51dfa226b463eac3a130283e6bc25f53a4ff695a49b91bbb0`, ordered `020c6518b722c83e650fd4236f15b32c96a41998b8782fc9e0997c1103931a32`; blocked admitted `0x1.4e49899996800p+2`, replay snapshot exact |
| Production `884b01e`, first 48 ticks | 32.2134 | 26.6016 | 6,240; same bank as frozen first48, ordered `6fda2236b013572644ae8b9d3be73dd050e818d2bd98e1f1d8c0a8a4ecd2bd27`; active, admitted 36° |
| Production, complete 360° | 22.970¹ | 19.015 | 2,600; same bank as frozen turn, ordered `0006289193fbe1ce51470db1045a20a139ffcc519851b8c18ba21868d4111a06`; completed 360° |
| Production, same stop and restore/replay | 17.430¹ | 13.445 | 398; same bank/admission as frozen stop, ordered `56f631d8de590440f1b4fbe7f9a2c2c3779304212224c10efd7aa1d95c122c5c`; replay snapshot exact |

¹ Baseline process-CPU value from the immediately preceding accepted standing-cache cycle on the same pinned program identity, documented in its archived `evidence.md`; the exact bank and ordered sample digests were reproduced by this candidate. The first-48 pairs above were rerun directly against baseline `a500a99` in this cycle. A second frozen first-48 bracket yielded baseline 29.088 versus candidate 25.132 seconds; the observed candidate gain therefore varies with host load but is material in both pairs.

Measured opportunity before mutation: the frozen lower crank Bound has 126,032 identity-DAG nodes but only 1,479 moving nodes, with 7 structurally duplicate moving nodes. Across 88,400 `_PathValue.bind` calls in two default ticks, structurally duplicate nodes are 6,536 of 996,950 node visits (0.656%); structural common-subexpression elimination was rejected. A separate one-tick exact-input audit found 41,490 of 44,200 binds repeating the immediately preceding finite IEEE inputs on the same path object, covering 457,824 of 498,475 bind node visits (91.8%). The path-local read-only prototype improved first48 tick CPU 29.088→23.685 seconds (18.6%) with exact 6,240 ordered samples and bank, which justified the bounded implementation cycle.

Red-first focused tests failed 2 of 4 cases on planning commit `40931d5` because an identical rebind still walked expression nodes; the other two cases pinned existing fallback behavior. After implementation, the focused evaluator/path/Bound/running gate passed 193 tests and 122 subtests. The complete top-level framework gate from this isolated worktree was `taskset -c 14 env OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 SOLID_BUILD_DIR=_build_checks /home/asa/devel/machinome-studio/.venv/bin/pytest tests/test_*.py -q`: **3,570 passed, 4 skipped, 2,121 subtests passed** in 381.57 seconds, with 53 nonfatal warnings. `openspec validate reuse-identical-path-binds --strict` passed, and `git diff --check` found no whitespace errors. Independent read-only review found no blocker and independently ran 15 focused tests; it confirmed finite IEEE key completeness, full eager fallback and successful-`bind_from` invalidation. Its wording clarification is reflected here: a failed bind retains the **last successful** entry, not necessarily the immediately preceding call's inputs.
