## 1. Prove the failure

- [x] 1.1 `tests/test_docs_structure.py`: no manual page and not the README
      says `AGPL-3.0-only`; run it red.

## 2. Restate

- [x] 2.1 Say `AGPL-3.0-or-later` wherever the manual, the README, the
      `pyproject.toml` comment and the architecture page state the viewer's
      licence.

## 3. Record

- [x] 3.1 Tests green, manual builds with `-W`; validate, sync the delta,
      archive, commit.
