## 1. Establish the duplicate replay and red contract

- [x] 1.1 Add a focused paired Follow-Bound fixture proving identical prefix probes currently rerun; extend the negative cases during implementation.
- [x] 1.2 Run the focused test red against the recorded base (157 Follow calls; assertion requires under 120) and preserve the failure output in the cycle evidence.

## 2. Reuse only proven successful prefixes

- [x] 2.1 Add a `_reached`-local successful prefix snapshot for the exact deterministic law-to-Follow subprogram and IEEE fraction, leaving Bound level/search code in place.
- [x] 2.2 Cover one-sided cut neighbors, source/Bound failures, restore/retry, different programs and two ticks with green focused tests (5 focused cases; 240 adjacent running cases).

## 3. Verify origin and complete the cycle

- [x] 3.1 Benchmark identical project source and five 0.1-second production Curta crank ticks before/after with wall and CPU times (32.916 to 19.307 CPU seconds on CPU14), identical complete 214-coordinate bank SHA `5d22f014…`, and 138 identical ordered raised-stop sample bits plus exact bank/snapshot replay. The project's four radial-ball tests pass in 289.445 seconds.
- [x] 3.2 Run relevant framework suites (240 targeted tests, full pytest 3599 passed/4 skipped/2131 subtests on base `83aad09`) and supported OpenSpec validation; rerun against the concurrent exact-guard integration before fast-forward.
- [x] 3.3 Sync the simulation requirement, archive this validated change, assess ADR need (none: private local reuse only), and complete the implementation record; reconcile with concurrent exact-guard main and integrate only after combined gates.
