## 1. Declaration and resolution

- [x] 1.1 Prove Slide and explicit coordinate selection fail through the public declaration/Sim path, covering the Curta-shaped crank and selector fixtures.
- [x] 1.2 Implement Slide and keyword-only coordinate selection for Button, Turn and Slide; make valid, invalid-domain, foreign-reference, multi-coordinate and unchanged-inference tests green.
- [x] 1.3 Prove controls change neither bank, source edges, program identity nor outcomes; exercise an interlocked slide and preserve ordinary move/trigger semantics.

## 2. Publication and consumer handoff

- [x] 2.1 Write failing export tests for prismatic buttons, signed slide ratios, selected placement ownership, offset pivots and non-parallel joints under moving ancestors.
- [x] 2.2 Publish the selected operation span from actual placement ownership; make publication and frame tests green while keeping legacy inferred control tables byte-identical.
- [x] 2.3 Export deterministic small consumer fixtures and record a reproducible generation command and exact producer content; verify no-control documents and the running conformance corpus remain unchanged.

## 3. Evidence and completion

- [~] 3.1 Run targeted and proportionate framework regression; obtain paired viewer gesture evidence from slide-and-turn-parts and record originating Curta coverage or remaining project blockers honestly.
      Framework regression run and green. The paired viewer evidence is
      NOT obtained: `slide-and-turn-parts` is proposed and unimplemented,
      and the Curta is another agent's assignment this cycle did not
      touch. Both are recorded as outstanding in `evidence.md`.
- [x] 3.2 Update public documentation, extract the confirmed successor decision to ADR-112 and update its index and architecture synthesis; state the paired viewer requirement without a release claim.
- [x] 3.3 Sync and archive through OpenSpec, run final validation, and create the single completed implementation commit after the planning-only commit; verify the two-commit branch and report local integration state.
