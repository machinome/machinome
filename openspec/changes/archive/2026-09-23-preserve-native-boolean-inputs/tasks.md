## 1. Red-first input preservation

- [x] 1.1 Add focused tests proving Common, Fuse and guard Section receive private copies of both operands in the existing default kernel mode; run them red on the unchanged framework and rejected protected-mode candidate.
- [x] 1.2 Reproduce the original Curta repeated-native-drum failure at its exact pose using a process-local cache wrapper over the committed fresh-native reader, and retain the fresh-pose control and tolerance provenance.

## 2. Narrow exact-layer change

- [x] 2.1 Deep-copy both operands for framework Common, Fuse and independent Section without changing default kernel mode, arguments, thresholds, error or result policy.
- [x] 2.2 Run focused exact geometry and ordinary disjoint, tangent, overlap, false-empty and failure controls, including the external 169° protected-mode regression.

## 3. Originating caller and completion

- [x] 3.1 Validate all ten height-minus-3 Curta native boundaries with repeated original drum operands, including the previously invalid midpoint, against the project's fresh-native default-kernel control with bit-identical boundary records.
- [x] 3.2 Run the full framework suite and relevant production project tests; measure copied-default cost and record exact pins, rejected protected-mode evidence and limitations.
- [x] 3.3 Sync exact-geometry specs, record the architecture decision if warranted, archive evidence, commit the implementation, and integrate only from the verified unchanged framework main.
