## 1. Establish evidence

- [x] 1.1 Pin a stable Curta project graph and measure baseline tick CPU and exact 213-coordinate state.
- [x] 1.2 Add a red test showing numeric path extrema unnecessarily enter generic face classification, with operand-order, signed-zero, NaN and error parity cases.

## 2. Implement and validate

- [x] 2.1 Dispatch numeric path min/max through the existing numeric operations and make focused tests green.
- [x] 2.2 Recheck the same Curta graph and exact bank; measure tick CPU and run expression, source-timing, carry, stop and exact-geometry regressions.
- [x] 2.3 Sync baseline spec, archive change, validate, commit the completed cycle and integrate only on verified framework-main head.
