# Curta boundary-witness correction

Originating project: `projects/Calculators/Curta-Type-I-3x`, unchanged source `FittedCarryBankFrameBench`, framework base `83aad09`. Project diagnostic `_build_checks/carry-guide-contact-fixed-a139fe0.jsonl` records three result-station and three turns-station slider/guide poses. At result station 1 drop 2.1 mm, the original guard refused native common 0 with candidate `(60.22464937443688, -7.8900000000000015, 20.24964937443688)`. Nearest planar face distances are 2.6645352591003757e-15 mm (slider) and 7.105427357601002e-15 mm (guide), both faces have OCCT `BRep_Tool.Tolerance_s` 1e-7 mm. `SolidClassifier.Perform(point,0)` says IN for both; at 1e-12, 1e-9, and 1e-7 mm it says ON for both. Y offsets ±1e-7 mm classify OUT of opposite solids. Thus the original point is unresolved nominal planar contact, not reliable shared interior. The turns-side zero/drop2.1 refusals are the same class of false positive.

The planning commit `b09ca46` was strict-validated before implementation. Before code, the new focused tests failed 3/3 because native boundary-separation and fault handling did not exist; the real guide diagnostic already demonstrated the behavioral failure. The candidate keeps zero-tolerance IN as a necessary condition and then asks every boundary face of the classified solid for its native tolerance and point distance. A point at or within any face's tolerance is not a contradiction witness. This tolerance qualifies *evidence*, not an intersection verdict or a positive volume.

Using the candidate framework on the unchanged source shapes, result guide drops 0/2.1/4.2 mm return native common 0/0/0. Turns guide drops 0/2.1 return native common 0, while drop 4.2 retains one native common of 0.595349994535576 mm³. The original positioning-ball/frame outer radial Z−0.2 and +0.2 mm false-empty comparisons still raise `ExactCommonInconsistency`; +0.01 mm free control returns zero solids and zero volume. The original ball witnesses are separated from the nearest frame face by 0.000294 and 0.001333 mm, far beyond its native face tolerance. No project source was edited.

An independent project rerun pinned to candidate `exact.py` SHA-256 `1529c4c75fff7e1879752dee2d22597d0afb2262ebdfeeebf72cc09ea6ffc7dc` passed `OperatingRadialBallGeometryTest` 3/3 in 56.727 s and all six guide diagnostic rows in 9.581 s, with zero guide refusals and the same positive full-drop turns common. Project logs (not copied into framework) are `_build_checks/operating-ball-resolved-witness-616c7bd.log` SHA-256 `ba46c9c6f25534238b3af51a6c0b6112ef9bbf4f686e76938a321b2b68171e95` and `_build_checks/carry-guide-resolved-witness-616c7bd.jsonl` SHA-256 `1cc432835d158753f8cc8b8cd8d4a2e538899c8e004db915167fc66e643112dd`.

Measured on CPU13, 30 repeated synthetic planar tangent box comparisons took 0.868 s candidate versus 0.887 s on the prior guard, no meaningful difference. Three direct comparisons of the real result guide at drop 2.1 mm took 2.000 s candidate versus 1.349 s prior guard; the candidate continues the finite search after rejecting a false early witness, about 0.217 s extra per comparison in this pose. A single candidate guide cProfile attributed 0.183 of 0.397 Python-accounted seconds to six CadQuery bounding-box/AddOptimal calls; 648 native face-tolerance calls were negligible. No speculative face cache was added.

The canonical framework `pytest -q` gate on CPU12 passed **3600 tests, 4 skipped, 53 warnings, and 2131 subtests in 393.11 s**. That run started before one additional tangent-classifier regression was added; the final focused six-test file passes separately, and the implementation source was unchanged after the broad gate began. Strict OpenSpec validation also passes.

The finite stencil remains incomplete. A real overlap thinner than the native face tolerance could go unwitnessed after an empty Boolean; no positive Boolean result is changed or waived, and no unobserved empty is advertised as a proof of clearance.
