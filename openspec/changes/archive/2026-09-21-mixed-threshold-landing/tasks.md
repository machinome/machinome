## 1. Reproduce the existing-contract failure

- [x] 1.1 Add a red public-Sim analytic moving-threshold test with independently derived results; record the faithful Curta reduction and baseline regressions.
- [x] 1.2 Reproduce the ordinary input-9 carry request in the complete exported Curta browser worker; identify whether a separate viewer-owned correction is required.

## 2. Correct and prove local landing orientation

- [x] 2.1 Select the local far side using the crossed level's destination branch, retaining fixed-source evaluation, nearest-representable landing, absolute commit and genuine invariant refusals.
- [x] 2.2 Cover both relative directions, negligible and zero driven displacement, stationary thresholds, constraint observers, subsequent requests, restore/replay and transactional failure.
- [x] 2.3 Extend conformance fixtures without changing the document version or public API, and run focused and full framework regressions.
- [x] 2.4 Prove following-contact precision failure red, certify constant relative level conservatively without tolerance changes, and retain infinitesimal real departures, unsupported-case fallback and genuine atomic refusals.

## 3. Validate the originating machine

- [x] 3.1 Pass the faithful CAD-free Curta reproduction and complete higher-stack operating tests using real geometry and unchanged source carry laws.
- [x] 3.2 Validate the real exported Curta request in the browser against the tested framework/viewer content pair; track any viewer correction as its own repository-owned cycle.
- [x] 3.3 Record mechanical geometry gates and any remaining project limitations separately; do not adopt provisional print fit changes as a side effect of this fix.

## 4. Complete the repository cycle

- [x] 4.1 Record confirmed ADR disposition and update the architecture synthesis only where the implemented decision changes it.
- [x] 4.2 Synchronize the simulation baseline, archive the completed change and pass supported OpenSpec validation.
- [x] 4.3 Commit the complete implementation record as commit two, verify clean two-commit ancestry and report evidence and integration state.

## Post-archive integration runbook (4.4)

On the existing explicit pilot integration authority, recheck the unchanged
clean primary base, fast-forward locally, verify both commits and safely
remove only this registered clean worktree. This operation follows the
completion commit and is recorded in the originating project's handoff;
it is not an already-completed claim embedded in that commit.
