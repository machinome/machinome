## 1. Red first: the declaration and the untimed reading

- [x] 1.1 Add the `Bound` fixtures to `tests/running_project/machine.py`, in the module's own style — a `...Body` class declaring no time base and a running subclass over it (`class Gate(GateBody): time = Time.running()`) — with two module-level law helpers beside `tooth_window`: `pin_lift(knot)` returning a LAW FACTORY of the shape `law=` takes — a two-argument function of `(source_owner, target_owner)`, as `tooth_window` is, returning `lambda travel: 5 - 5 * clamp01((travel - knot) / 5)` (a lift of `5` until the travel reaches `knot`, `0` from `knot + 5` on, so the `[-0.05, 0.05]` window opens at `knot + 4.95`) and `cleared(value)` returning `abs(value) <= 0.05`. The machines:
  - `GateBody`: drivers `feed` (`10.0`, `'mm'`) and `turn` (`0.0`, `'deg'`), the defaults being where the running scenarios start; children IN THIS ORDER, because a `Bound` may only read what the body has already declared — `p1 = Slide(lift=Prismatic(axis=(0, 0, 1), unit='mm'))`, `p2` the same, `plug = Arbor(turn=Revolute(axis=(0, 0, 1), unit='deg', range=(0, Bound(lambda turn, a, b: 90 * cleared(a) * cleared(b), reads=(p1.lift, p2.lift)))))`, `key = Slide(travel=Prismatic(axis=(1, 0, 0), unit='mm'))`; then the relations `feed.drives(key.travel, ratio=1.0)`, `key.travel.drives(p1.lift, law=pin_lift(10))`, `key.travel.drives(p2.lift, law=pin_lift(13))`, `turn.drives(plug.turn, ratio=1.0)`. Every joint here is SITE-declared, as every joint in this module is, so the declarer is the root.
  - `CapturedBody`/`Captured`: a sibling of the same shape (not a subclass — the declaration order is load-bearing) whose `feed` defaults to `20.0` (the key seated) and whose `key` additionally declares `range=(Bound(lambda travel, turn: 20 * (turn > 0), reads=(plug.turn,)), 20)`. Check the rest pose of each body satisfies its own bounds: `Sim` construction renders it untimed and the enumeration's close would refuse it.
  - `GateWideBody`/`GateWide`: `GateBody` with the window at `0.5` instead of `0.05`, for the identity test and nothing else.
  - `PawlRatchetBody`/`PawlRatchet`: drivers `arbor` (`40.0`, `'deg'`) and `lift` (`0.0`); `pawl = Slide(lift=Prismatic(axis=(0, 0, 1), unit='mm'))`; `wheel = Arbor(turn=Revolute(axis=(1, 0, 0), unit='deg', range=(Bound(lambda turn, lift: 36 * floor(turn / 36) - 1000 * (lift >= 1), reads=(pawl.lift,)), None)))`; `arbor.drives(wheel.turn, ratio=1.0)`, `lift.drives(pawl.lift, ratio=1.0)`.
  - `ClassGateBody`/`ClassGate`: the other resolution path — an `AssemblyNode` subclass `Plug` declaring its own children `p1`, `p2` and its OWN class-declared `turn = Revolute(...)` whose `Bound` reads `p1.lift`, `p2.lift`, held in the root body as `plug = Plug()`. Here the declarer is the `Plug` node itself, not the root, and the ids the reads qualify to are `plug.p1.lift` and `plug.p2.lift`.
  - `PortReadBody`: a `Bound` reading a plain `RotationalPort` an author's `simulate()` binds — legal to declare, refused at `Sim` construction (task 4.1), so it stays a module-level class with no running twin used by `test_running_stops`.
- [x] 1.2 The four CLASS-DEFINITION refusals are class statements that must raise while the body runs, so they are written INSIDE the test methods (a module-level one would break the import of `machine.py`), in `tests/test_joints.py`: a `Bound` reading a declaration held in a list, a `.repeat()`ed child, a child whose class declares two joints, a driver read off a child declaration, and a read naming the bounded coordinate itself. Assert each names the read and the rule, and assert the message for the list and the repeat is the one the same end of a relation gets.
- [x] 1.3 The rest of `tests/test_joints.py`: realization carries a `Bound` unevaluated (`joint.arguments(node)[2]` is `(0, <Bound>)`, no read resolved, no number computed) for both a class-declared and a site-declared joint; the untimed enumeration-close judgement refuses `GateBody().set_state(turn=30, feed=0)` naming the node path, `plug.turn`, `30`, `'deg'`, the evaluated bound `0` and both reads with the value `5` each, and admits `set_state(turn=30, feed=20)` with both lifts at `0`; a read left unbound is not judged; the refusal is independent of the order the solver bound the coordinates in (assert it from both `GateBody` and `ClassGateBody`, whose subtree order differs); a `Bound` side is NOT applied at the moment of binding (a direct `node.plug.turn = 30` outside any enumeration places the body and raises nothing).
- [x] 1.4 Run the new tests and record them red: `Bound` does not exist.

## 2. The declaration, resolution and the enumeration-close judgement

- [x] 2.1 `solid_node/motion/joints.py`: `class Bound` taking `(expression, reads=())`, exported from the module's `__all__`. `__init__` turns each read into a reference through `couplings.coordinate_ref(value, role='read')` and calls its `check('driver')` — a read is a source, so a `BroadcastRef` through a repeat and a `PathRef` whose root is list-held are refused there, by the messages a relation's source already gets; `_DeclaringNamespace.__setitem__` has already named the declarations, so those messages read right while the body runs. `described()` for the messages that name it; `arguments(own, values)` applying `expression` to the own value first and then each read in declared order. `Bound` MUST NOT be callable: `Joint._span` tells a whole-range callable from a bound by position, and `_span`'s first test is `callable(declared)`.
- [x] 2.2 `solid_node/motion/joints.py`: `Joint._span` carries a `Bound` bound through unevaluated, beside `None` and a callable, and leaves the `low > high` ordering check alone (a `Bound` is not a number). `Joint._bound_at` returns `None` for a `Bound` — the deferral — so `_refuse_out_of_range` judges the other side alone and never compares a `Bound` object with a number. `Joint._refuse_out_of_range`, when either resolved bound is a `Bound` and the value being bound is a plain number, calls `phase.note_bound_binding(node, self, value)`.
- [x] 2.3 `solid_node/motion/joints.py`: the class-definition checks that need the declaring class. `Joint.__set_name__` runs, for every `Bound` in its declared range and for each read, `ref.check_declared_on(owner, bound)` and the refusal of a read whose declaration IS this joint's own coordinate — but only when `self._declared_at_site` is False, because `_specialize` fires a site joint's `__set_name__` against the specialized CHILD class, which is not the declarer. `solid_node/node/declarative.py`: `ChildDeclaration.__set_name__` runs the same two checks for every site joint in `self.site_joints`, beside `_check_wiring(owner)`, where `owner` IS the declaring parent.
- [x] 2.4 `solid_node/motion/joints.py`: `Bound.resolve(declarer)` — one `ResolvedEnd` per read, through each ref's own `resolve(declarer)`, cached per node and per joint name in a cache OF ITS OWN (`node.__dict__.setdefault('_joint_bound_reads', {})`), never in the `_joint_arguments` tuple, whose index 3 an `Orbit`'s carried point already occupies.
- [x] 2.5 `solid_node/node/phase.py`: `Enumeration.__slots__` gains `'bounds'` and `__init__` the third list; `note_bound_binding(node, joint, value)` appending `(node, joint, value)` when a SIMULATE phase is running and an enumeration is open, and doing nothing otherwise — the shape and the silence of `note_unbound_read`.
- [x] 2.6 `solid_node/motion/couplings.py`: `refuse_bounds(enumeration)`, exported beside `refuse_reads`. For each recorded binding: skip a slot `run_owned` reports (the run judged it — belt and braces, since a run's coordinate delivery happens outside any enumeration and the solver leaves run-owned slots alone); resolve the reads against the DECLARER — the node for a class-declared joint, `node._parent` for one whose `_declared_at_site` is True; skip the binding when any read holds no value or a value that is not a plain number; otherwise apply `Bound.arguments`, order the pair, check the value inclusive, and raise `JointRangeError` naming the node's path (`_where`), the joint, the value, the unit, the evaluated bound and every read with its own `described()` and value. `solid_node/node/assembly.py`: `_finish_enumeration` calls it after `refuse_reads`.
- [x] 2.7 Section 1 green; `tests/test_joints.py`, `tests/test_couplings.py` and the whole existing untimed suite green unchanged.

## 3. Red first: the running reading

- [x] 3.1 In `tests/test_running_stops.py`: every running scenario of the simulation delta, over the fixtures of 1.1 — blocked with nothing admitted while a pin crosses; turns once clear; insertion and turning in one tick; withdrawal from a turned plug stopping the key within the crossing tolerance below a travel of `17.95` and leaving `plug.turn` at exactly `30`, the stop naming `plug.turn`, `'high'`, the evaluated bound `90.0` and `('feed',)`; the declared capture blocking at once with `0` admitted; return-and-withdraw in one tick; the pawl at `0.3` and at `0.5`; cadence independence of the withdrawal over 1, 4 and 40 ticks; a snapshot replaying a constraint stop identically; `Gate` and `GateWide` differing in `program.identity`; `PortReadBody` refused at `Sim` construction naming the port and the joint; `ClassGate`'s reads compiling to `plug.p1.lift`/`plug.p2.lift`; a `Bound` reading a driver evaluated as an input; a stop record's `value` being the bound at the COMMITTED state rather than the coordinate's own value.
- [x] 3.2 Replace `DeferredBoundTest` in `tests/test_running_stops.py` — the `@skip('design.md section 10: ...')` that pins ADR-109's deferral — with the real test, so the deferral cannot stay pinned open.
- [x] 3.3 A `Train` cost probe: the deterministic count of graph evaluations per tick is unchanged and the wall time is within noise of ADR-108's 1.065 ms/tick record.
- [x] 3.4 Run and record red: `_compiled_bound` refuses a second free name today, by the very message this change removes.

## 4. Compile and run

- [x] 4.1 `solid_node/simulation/program.py`: `_compiled_spans(root, coordinates)` — the ROOT is a new first argument, needed to qualify a read's owning node through `instance_path`, and `compile_program`'s one call site passes it. For a `Bound` side it resolves the reads against the declarer, qualifies each `ResolvedEnd` to the id the bank keys by (`driver_id(instance_path(end.node, root), name)`, the same path `_qualified` takes), refuses by joint and node identity a read that is not a key of the bank — a plain port, a derived coordinate — saying a bound reads the state and naming the joint the port follows, refuses two reads qualifying to one id, applies the callable to `symbol(own_id)` and one `symbol(read_id)` per read in declared order, and walks the graph for raw text and for calls outside `SYMBOLIC_BUILTINS` exactly as the one-argument path does. The free-name check becomes `names <= {identifier} | set(read_ids)`, keeping its refusal for anything else. Jumps stay admitted with no plan. `_compiled_spans` returns the same 4-tuples and additionally, per bound, its read ids.
- [x] 4.2 `solid_node/simulation/program.py`: `Program.__init__` builds the constraint table AFTER `self.edges` and `self.sources` — `Constraint(identifier, side, graph, reads, edges, candidates)`, where `edges` is the sub-program (the compiled edges determining `{own} ∪ reads`, walked back through `self.determiner` and kept in `self.edges`' own order) and `candidates` the union of `self.sources[key]` over those same keys. `described()` keeps its exact form, so the identity changes only through the graph's own free names; `published()` and `_published_bound` are untouched.
- [x] 4.3 `solid_node/simulation/run.py`: `Run._bounds()` evaluates a self-only span as today and hands a constraint through unevaluated; `Run._reached(held, committed, bounds)` keeps its test for self-only spans and, for a constraint, first asks whether the bounded coordinate or any read has a nonzero delta over the stretch (from `committed - held`, no evaluation) — if not, nothing; if so, it runs the sampled search of 4.4 and reports the constraint as reached, carrying the located `t*` so `_locate` does not search twice.
- [x] 4.4 `solid_node/simulation/run.py`: detection and localization as one procedure, `_searched_constraint`: sample the level `g` at `_SUBDIVISIONS` fractions of the stretch, each sample one pass over the constraint's sub-program edges with every admission scaled by that fraction (a fresh delta map per sample, never `_pass`'s, which mutates and raises), the own coordinate in the bound frozen at the tick's committed value; stop at the FIRST sample `t_k` with `g(t_k) > 0` and `g(t_k) > g(0)`; `g(0) > 0` with a higher first sample gives `0.0`; otherwise bisect `[t_{k-1}, t_k]` to `_CROSSING_TOLERANCE` in at most `_BISECTION_ROUNDS` rounds and return the INSIDE end of the final bracket; no sample positive means no stop. `_locate` uses the `t*` `_reached` already found for a constraint.
- [x] 4.5 `solid_node/simulation/run.py`: the group and the commit. `_group` takes the constraint's own candidate list rather than `sources[key]` and filters it with `_carries(candidate, delta, constraint, values)` — the candidate's admission alone, over the sub-program, raising the level from start to end — leaving `_pushes` untouched for a self-only stop. In `integrate`, the snap `committed[identifier] = bound` is skipped for a constraint, the `Stop` entry's `value` is the constraint's bound evaluated at the committed state, and after the segment the level at the committed state is asserted at most zero, raising `StopInvariantError` (the whole tick committing nothing) otherwise. `Stop`'s docstring in `program.py` is corrected: `value` is the evaluated bound, which for a self-only bound is also the value the coordinate now holds and for a constraint is not.
- [x] 4.6 Section 3 green; `tests/test_running_stops.py`, `test_running_simulation.py`, `test_running_jumps.py`, `test_running_document.py`, `test_running_corpus.py` green unchanged; the Pascaline module's `simulation/test_pascaline.py` and `tests/test_document.py` green against this worktree, its document byte-identical.

## 5. Publication and the corpus

- [x] 5.1 `tests/test_running_document.py`, red first: `Gate`'s document publishes `program.spans['plug.turn']['high']` as an `{"expression": ...}` whose free names — read through the document's `bindings` table, by the module's own `resolved()`/`free_names_of()` helpers — are drawn from `plug.turn`, `p1.lift` and `p2.lift` -- the two reads, the fixture's expression not naming its own coordinate; `ClassGate`'s likewise from `plug.turn`, `plug.p1.lift` and `plug.p2.lift`, the ids the export delta's own scenario names. Every one of them is a key of `program.coordinates`, `low` is `0.0` and the document's `version` is `5`.
- [x] 5.2 `tools/generate_running_corpus.py`: `REQUIRED` gains exactly `'a bound reading another coordinate'` and `'a stop reached by the motion of what a bound reads'`, the two names the export delta lists. `uncovered_features` detects the first by reading each published span side's expression through the document's `bindings` table and finding a free name other than the span's own key that is a key of `program.coordinates`; the second by comparing, for every recorded stop, the bank value of its coordinate in that tick's entry with the value in the previous tick's entry — and for the first tick with `program.coordinates[<id>]['initial']` — and marking it when the two are equal.
- [x] 5.3 `tests/test_running_corpus.py`: the refusal is asserted directly for each new feature, red first, as it is for a missing primitive.
- [x] 5.4 A `Captured`-shaped scenario in `CORPUS` exercising both — a script that turns the plug with the pins clear and then withdraws the key — regenerate `tests/running-corpus.json`, and confirm the committed fixture replays and that each machine's freshly published document still equals the fixture's copy.
- [x] 5.5 Green.

## 6. The originating project: the pin tumbler lock

- [x] 6.1 In `projects/Locks/Pin_tumbler_lock` (its own repository, its own OpenSpec change, run against this worktree's framework with `PYTHONPATH` set to it): tooling that emits the exact breakpoints of `lifts_for` per pin for the three keys and pins them against `lifts_for` at 601 stations to ten places; the migration of design.md's plan (running root, `insertion` and `turn` inputs, `d1`…`d5`, laws, the plug's `Bound`, the key's capture `Bound`, the driver pins' `(-6.9, 6.1)` spring range with its source-evidence note, the stepped instructions); the red-first running scenarios of design.md step 6 beside `SeatedScenario`; `contact.py` kept as the reference; `LockState`, `web/mechanism.mjs` and `test_parity.py`'s JS half retired with the record saying the browser acceptance waits on the viewer cycle.
- [x] 6.2 `tools/check.py` green under `--exact` and `--faceted`; pixels: `solid snapshot --drive` stills at withdrawn, mid-insertion (a pin riding a cut), seated, and turned; commit in the project's repository.
- [x] 6.3 Record in this change's `evidence.md`: the lock's blocked positions and admitted travel, the derived capture's `t*` and travel, the spring limits reached by no legal key, the document's spans, and the shipped viewer's by-name refusal of the lock's document.

## 7. Records

- [x] 7.1 `docs/adrs/NODE/ADR-113-a-bound-may-read-other-coordinates.md` (the number is free: NODE stops at ADR-109, EXPORT at ADR-111), ADR-109's consequence "A bound may NOT name a second coordinate in this release" amended to point at it, `docs/adrs/README.md` indexed, and `docs/architecture.md`'s simulation section updated — including line 973's known gap, "a range bound may not name a second coordinate", which this change removes.
- [x] 7.2 `workflow/warts.md`: the lock's naming sighting (`.repeat()` children owning a joint under a running root) recorded with the supported answer; the cancellation wart left as filed.
- [x] 7.3 OpenSpec sync and archive; full suite green; the completed record committed as commit 2.
