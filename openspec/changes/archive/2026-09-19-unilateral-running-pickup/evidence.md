# Implementation and adversarial review

## Identity and authority

Origin: the complex twelve-bolt Vault with Combination Lock, project checkpoint
`78d4cebda78d417c3ff4a4e439cea0e801088dc0`. The pilot explicitly selected
`Time.running()`, asked separate Sol proposal/implementation agents, adversarial
review, local main integration, and continued project work; the subsequent
explicit GO ratified proceeding without another routine approval pause.

Standalone framework base: `b9b64ddaf0bc1d51d715d3d971b77b6ee58880bf`.
Planning-only commit: `e99e7121bb2b486a5c3d60b3256b19ea8999d0a7`.
Bench/branch: `WTs/unilateral-running-pickup`, integration target `main`.
The shop's two-commit cycle overrides the stale per-increment guidance returned
by local OpenSpec context. The viewer is an independent AGPL repository, not
framework source. Nothing here authorizes pushing or publishing.

## Red evidence

At the base framework, the new independent test cannot import `Play`. The
unchanged originating project has four running contracts: one pass, the
mesh-free reversal fails (1577 versus retained 1587), and two measured-chain
contracts raise `TooManyCrossings`/sliding `UnsupportedLaw`. The original
callable spike remains unchanged and still reproduces its failures across
cadences. This is a missing opt-in capability, not a regression in ADR-121.

Root's independent `test_running_play_review.py` caught intermediate defects
before they were fixed: downstream stop localization from net follower travel;
large-angle cancellation between chained edges; a second terminal writer;
stop recollection through clearance; affine, nonlinear and multi-source
downstream observers; and the malformed different-target declaration falling
through to a nonexistent ordinary `forward` operation. Negative corpus
mutations remove behavior while leaving the play declarations present.

## Validation

Sol's broad framework run: 3422 passed, 4 skipped, 1999 subtests, 53 existing
warnings, 380.85 seconds. After the subsequent focused declaration and
secondary-path refinements: 148 passed/170 subtests across play, review,
corpus, self-read and stops; the broader focused document/API set passed
269 tests/326 subtests. Final malformed-declaration review validation is
recorded in the closeout below.

The original self-read control remains 42 tests/62 subtests. The independent
measured direct-dial caller proves the Vault's three offsets under reversal,
partial and multi-turn commands, several cadences, downstream peg-style
observers, snapshot/restore/reset and an independent geometric oracle.

Corpus: 22 scenarios, 19 machines, 378 ticks; SHA-256
`3f3407c11f321e88d626565d97d43ff56ea63a6c1c1d19bfe8ca028cbd46eed6`.
All twenty prior no-play scenario objects compare exactly equal to the base.
There is no widened tolerance. Version 9 is selected only by an explicit play
edge; an old viewer reporting only versions 1–8 refuses it.

The independent viewer companion is integrated locally as
`038f74d719a53ef17c807b6bd007406fa3512c11`, planning commit
`091cb93d2ba4d6b7e26b27a2f1c8966d078f0b17`. Its broad validation passed 1242
Vitest tests and 183 Python/browser tests with 18 subtests; the final focused
corpus/run set passed 88 tests. A mounted Chromium page executes a version-9
request and observes its follower bank. The installed command reports API 22 and
document versions 1–9. Its corpus is the byte-identical file named above.

## Records and limitations

ADR-131 records the implemented choice and rejected alternatives; the
architecture, ADR index and public running documentation are updated. All
three delta specs have been synchronized into the existing baselines, whose
34 specs validate strictly. The local wart and historical investigation are
retained under `workflow/`; GitHub CLI is unauthenticated, so no external issue
has been posted and no issue number is claimed.

This closes the framework capability, not the mechanical Vault. Its source
orientation, published 25–52–17 sequence, fence/cam engagement, twelve-bolt
train and complete door/box assembly still require project-owned proof.

## Final closeout

After the last early-declaration refusal fix, Play/review/coupling tests pass
205 tests and 197 subtests; the running simulation/read/stop suites pass
201 tests and 111 subtests. The final focused Play rerun passes 23 tests and
20 subtests. Root's review additionally checks tick rollback after an injected
post-projection refusal, cycle/wrong-target refusal, and three behavior-removal
mutations of the corpus. No integration occurred with a known failing test.
The earlier full-suite count is reported separately from these final focused
runs; it is not represented as a second full run after the last small fix.
Root's final post-fix Play/review/corpus run passes 39 tests and 86 subtests.
