# Curta traced-constraint path evidence

Originating caller: independent Curta-Type-I-3x `OperatingCurta`, frozen clean detached project worktree at commit `7586002` (`WTs/perf-758-reference`). Its compiled program identity is `be125f4048c8be00ce4e73d646311d4a50ac75a59aa739f93bf2f38c992fa195`; the aggregate crank-low graph has 23 reads and 126,032 reachable DAG nodes. In the two actual traced searches on the active stop, two read names move and the moving cones hold 7,851 and 3,670 nodes. The project source hashes match the preceding `compile-moving-path-evaluation` archive: `running.py` `bf32eeaf6ffd4fae3a038889af17d38616ab6a0824e8dc38b4f3c106150a825a`, `running_parts.py` `9b7f755ee9c33fa12cb2b8a6b341153b454472af8c01e061a23cdb8aedeae1f8`, `result_bank_lockout_parts.py` `7b6a709ea64878a7ee37df6ce454ca17a772bb7ad96d7cf1c3776f804aa67b82`, `higher_locking_laws.py` `561ec3b96185b6a44f26012a3db2c9bd1013de3a868a4c9ad8af03cf7d6372c6`, and `higher_locking_profiles.py` `1cf2bed3b1be41ba66bccc2eb788fb24ddc5c21698853478e8d0f02813e10529`.

The command sequence in both processes was `Sim(OperatingCurta(), dt=.1)`; completed zero-duration moves `digit_3→3`, `crank_rotation→160`, `digit_3→0`; then `move('crank_rotation', to=170, duration=.1)` and `run(.1)`. Each process used the workspace venv, `OPENBLAS_NUM_THREADS=1`, `OMP_NUM_THREADS=1`, `SOLID_BUILD_DIR=_build_checks`, identical frozen project `PYTHONPATH`, and `taskset -c 14`. The parent confined its long-running tests to CPUs 0–11; other user processes were not controlled. Times are one sequential unprofiled A/B pair, not a distribution.

| Framework | Preparation CPU / wall (s) | Active tick CPU / wall (s) | Stop |
| --- | ---: | ---: | --- |
| Base main `4d24d79` | 32.038 / 32.760 | 8.579 / 8.586 | Blocked at `165.22323837279146` |
| Candidate `a9ebe85` plus source diff | 27.230 / 27.853 | 6.252 / 6.256 | Blocked at `165.22323837279146` |

The candidate saves **2.327 CPU seconds (27.1%)** on the active stop tick. Both have 213 state entries and exact canonical JSON SHA-256 `f5c67383a964a55ded731637e8612efffd1938725f7ec0417fc85d6e4cb6f1a9`. Preparation status was completed for all three commands. No project code, sample count, tolerance, dt, or law changed. The change improves one active tick but does not by itself make a complete Curta turn interactive.

Actual-source sample trace: instrumentation captured, within `_searched_constraint`, each bound graph's ordered `(search index, sorted argument names and IEEE-754 float bits, bound result float bits)`. Baseline `GraphValue.evaluate` and candidate `_PathValue.bind/at` made the same two active-tick searches and **134 samples**; their complete ordered trace JSON SHA-256 is the same, `6663f44513aff8a7aead966c643e770025d3909a4651c942db89d8b7f8236740`. A separate fresh-instance prototype had matched all 264 graph calls across preparation plus tick. A same-`Sim` restore comparison initially yielded different search counts and was rejected as an invalid parity method; all acceptance comparisons use fresh instances.

Red-first: two focused tests failed before implementation because the traced search never invoked `_PathValue.at`. After the change all 11 focused tests passed, including standing refresh, first-sample eager arithmetic error, opposite signed zeros, NaN and nontraced replay. A broad running/clocked/source-timing/carry/stop/expression group passed **508 tests**. Strict change validation and all 34 strict baseline specs passed. Independent read-only review found no blocker after checking the traced gate, t=0 eager bind, own-coordinate freeze, nontraced prefix replay, conservative moving classification, signed-zero/NaN treatment and graph lifetime; its separate 61 relevant tests and 25 subtests passed. The architecture synthesis now names this additional ADR-124 call site; no new ADR is needed because the choice reuses the existing tick-local path discipline rather than changing architecture.
