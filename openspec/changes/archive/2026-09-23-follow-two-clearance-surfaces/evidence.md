# Curta retained positioning ball evidence

Originating project: Curta Type I, `/mnt/data/machinome-projects/Calculators/Curta-Type-I-3x`. The project body for final producer checks is Git `4cd6ec4373fe95418eaa8a1539675c2c8a894c07`. The root-owned uncommitted trial and measured profiles were imported read-only, with SHA-256:

- `simulation/positioning_ball_trial.py`: `95da88263bb4e2ef0a6eba82c84b5e7e8ecb371974c101aac1da538e0a0a36f1`
- `simulation/positioning_ball_profiles.py`: `cf0382d6cea269bff7c66dfdf8651bd857f5457396ffec80778fe6ca7d591af4`
- `simulation/test_radial_positioning_ball.py`: `fe21c9645628499c8ecfe2f92ec4d9d0d9da1843212dbd5f098c28ff720bb432`

The unmodified project's switch-expression law fails its two scoped tests: on bell return the ball is pulled from its outward position to `-1.2957251833030412`; with lift held at 6 mm a coarse crank request blocks at 0° instead of the measured positive contact interval. The parent project log is `_build_checks/radial-positioning-ball-root-constraints-a2d0783.log`, SHA-256 `f224537f9d0d6c9ca8cb19cf6c3f3a687211697956aee34bf0f1ee819b2312b5`. The same failures were reproduced read-only against this cycle's starting framework in 92.416 s. `max(lower, min(travel, upper))` as an ordinary self-read is correctly refused under ADR-121; a single-source fixed-offset `Play` cannot express the two measured curves.

The standalone probe `curta_follow_probe.py` declares a temporary `FollowTrial` subclass using the project's actual bell/collar expressions and existing two dynamic Bounds. It does not edit project source. Run it from this framework worktree with `PYTHONPATH=<this worktree>:<project root>`, workspace `.venv/bin/python`, `OPENBLAS_NUM_THREADS=1`, `OMP_NUM_THREADS=1`, and an absolute worktree-local `SOLID_BUILD_DIR`.

Synthetic red/green fixtures in `tests/test_running_follow.py` cover interior peak retention, sub-1/64 inversion missed by every uniform probe, closure-only one-ULP positive without a representable positive neighbor, long modulo request whose uniform probes all alias home, signed-zero quiet retention, opposing source attribution, snapshot replay, missing/mismatched Bounds, invalid rest and relation, terminal-output refusal, curved-path atomic refusal, and wiring source refusal. The exact v12 jump-plan program fragment is pinned in `tests/fixtures/follow_wrapped_v12.json` and asserted against fresh producer serialization.

`tests/fixtures/follow_wrapped_commands_v12.json` pins the ordered push/retreat/opposite-surface commands and complete synthetic banks. The producer test executes every row, and the viewer companion copied it byte-identically for its independent command parity gate.

The project's actual bell graph has a cut at 6° where the existing increment-integrated left-piece reading is `0x1.c7815181b9204p-5`, while the authored right/exact reading is `0x1.c7815181b9200p-5`. Follow uses existing path compilation for certified cuts and affine shape, but evaluates the authored branch skeleton over the exact source line for absolute contact values. No `_agree` tolerance hides this difference.

With the final exact-source-line refinement, the full read-only project probe passed again after the whole-framework gate: 0→90° over 0.5 s completed with ball at `2.213142830078919`; 90→360° over 1.5 s completed and retained that value exactly. At lift 6 mm the ball reached `-1.2252018554755857`; crank 0→90° over 0.5 s blocked in the first 0.1 s at `0.36119713971311285`; restore and replay gave the same bank/snapshot; lowering the carriage relieved the stop and the turn reached 90° with ball `2.213142830078919`. The synthetic Follow suite passed 18/18 and the relevant existing running suites passed 251/251.

The first whole-framework run found only the static public-export inventory expecting the pre-Follow names (3587 passed, 4 skipped, one failure). After adding `Follow` to that inventory, its focused package/Follow run passed 54/54. The clean full command `taskset -c 14 env OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 SOLID_BUILD_DIR=_build_checks PYTHONPATH=. /home/asa/devel/machinome-studio/.venv/bin/python -m pytest -q --tb=short --disable-warnings` passed **3588 tests, 2124 subtests, 4 skipped** in 404.14 s. Strict `openspec validate follow-two-clearance-surfaces --strict` passed before archive. An independent framework reviewer found no remaining source-path or wire blocker; the narrowed exact-line ancestry and unchanged ordinary/Play/Bound paths were inspected.

The paired viewer consumer independently loaded a fresh actual Curta version-12 no-mesh document (SHA-256 prefix `aff53651`) and compared all **214 coordinates** at eight Python oracle stages: rest, raised lift, blocked crank, restored replay, relief, after-relief crank, outward quarter-turn, and complete return. Status, stop at `0.36119713971311285`, replay snapshot, and every bank value matched exactly; the companion oracle SHA-256 prefix is `d1a428e`. This is a paired local producer/consumer test, not a claim that the viewer package has been integrated or released.
