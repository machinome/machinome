## 1. Red evidence

- [x] 1.1 Add a minimal public running-node regression that copies an inherited control across a compatible same-name child replacement; prove current class definition fails.
- [x] 1.2 Add negative controls for foreign same-name part/coordinate, incompatible child, missing effective nested path, wrong selected domain/ancestry, and explicit table replacement; pin current refusals where applicable.

## 2. Narrow producer correction

- [x] 2.1 Admit only ancestor-owned, unchanged controls across compatible same-path replacement at class validation; retain exact checks for ordinary controls and drivers.
- [x] 2.2 Ensure effective part and selected coordinate resolve through the replacement at compile, with named fail-closed errors for missing/incompatible paths and no stale declaration publication.
- [x] 2.3 Run focused control/enumeration/program/document tests and broader relevant framework suite; verify unchanged legacy control serialization where the model did not change.

## 3. Originating caller and completion

- [x] 3.1 Import the unmodified project `LoopOperatingTrial` with the isolated framework, compile and enumerate inherited plus new controls, exercise a representative gesture, and inspect the produced document with the existing viewer contract. Record project source and framework pins.
- [x] 3.2 Record honest acceptance/limitations, decide whether an ADR is warranted, sync the simulation spec, archive the change, and run final validation before the implementation commit.
