# Paired source-timing completion evidence

2026-09-22. Pilot authority: autonomous evidence-led correction, not push or
publication. Framework base `e6a42c80e6dcc686c180b8a6d94037301c4213a5`, planning
commit `95478bb`; standalone integration target `main`. Viewer base
`c8da56e77f7653a06b335459e7759c6b24152e69`, planning `46fea1d`. Each repository
owns its implementation and records; no viewer source enters the framework.

Viewer completion commit: `684c58338800ad300e0d2594cc8d17d61cb3a7f5`.
Its worktree is clean and exactly two commits above its recorded base.
Post-archive compact/version/path/full-Curta rerun: **33 tests passed**,
65.81 s. Its archive is
`openspec/changes/archive/2026-09-22-preserve-running-source-timing/` in the
independent viewer repository.

## Outcome and proofs

Final post-archive combined producer run: **790 passed, 797 subtests passed**,
56.33 s, three warnings. The one skipped check is the explicitly opt-in real
viewer capture (run separately below). This includes all running, relevant
contact/constraint, document/corpus, export, browser-staging, snapshot and
manual-structure/link checks in one process, with no scratch-directory races.
The opt-in `BrowserSnapshotEndToEndTest`, with both isolated checkouts on
PYTHONPATH and `MACHINOME_WEB_SNAPSHOT_E2E=1`, separately **passed** in 2.33 s
through the real viewer process (one existing Pillow deprecation warning).

The unchanged Curta six/seven/eleven result banks and constrained full bank
all complete digit 0, height 9, crank 90, then bulk crank 180 with ones 724,
tens 704 and first lever 0. Their full banks agree with twelve restored
portions under the existing agreement rule. No project law, oracle, command,
constraint, bank or default changed. Generator outputs retain full records;
the independently consumed viewer copies are pinned by hash in
`evidence/paired-source-timing.json`, which also records full final banks.

The same file identifies exact tested production, generator, fixture and
viewer bundle content. All five new fixtures match the current producer
hashes. Curta is unchanged checkpoint 8852677. Compact cases cover ordinary
and selected carries, landings, later selectors, nonuniform and curved
sources, ranges, contact observation, reverse/replay and crossing budgets.

- Framework running/document/corpus/clocked-document/viewer gate: **581 tests,
  696 subtests passed**, 52.26 s, two existing render/driver warnings.
- Additional contact, following, ancestor/periodic constraint and docs checks:
  **67 tests, 91 subtests passed**, 2.56 s. Obsolete manual-link test paths
  predated this change; corrected to the existing manual hierarchy and restored
  direct operation/API links, red first.
- Unchanged originating-project regression: **4 passed**, 89.878 s at the
  earlier candidate. Final semantic-generation producer fixtures independently
  re-run all four bulk/partition gates: 30.247, 35.306, 50.954, 62.235 s total
  per case. Concurrent wall times are observational, not benchmark guarantees.
- Viewer: **1456 TypeScript tests in 55 files passed**, 78.49 s; typecheck
  passed. Whole legacy running, clocked and time-drive corpus JSON is retained.
- Chromium worker/fallback plus carriage checks: **8 tests and 2 subtests
  passed**, 31.22 s. Both diagnostic captures inspected at 180/724/704/0;
  stand-in carriage captures inspected separately. The diagnostic explicitly
  contains no native CAD geometry.
- Complete viewer Python/browser suite: **196 tests and 22 subtests passed**,
  248.52 s, 51 existing dependency deprecation warnings, no skips or failures.
- Viewer distribution smoke test passed, installing outside the checkout and
  reporting API 24/v1–11; no upload. The actual old API-23 bundle rejects v11
  in Chromium, and the framework entry-point capability gate rejects it too.
- Every new running export selects v11; posed/looping and clocked selection
  is unchanged. Endpoint-era snapshots refuse atomically; corrected replay
  passes. Legacy publication comparisons normalize only version and identity.
- Strict OpenSpec validation passes for both repositories. ADR-137 records
  path composition and ADR-138 the semantic gate; the architecture and public
  migration guide are updated. Simulation and export deltas are synchronized.

## Additional findings handled

Inherited source pieces initially renewed the per-law crossing allowance.
Red tests in both runtimes now refuse over-budget propagation transactionally,
including record-disabled probes. Endpoint timing assertions changed only
where physical evidence requires it; old corpus files were not regenerated.
The browser RangedBlock stop is exactly .3/.6 rather than the old searched
approximation. Its carriage cost is 6277.4 rather than 5913.2 evaluations/tick;
Train and Clearing controls retain their counts. General curves retain the
existing bounded-search limits; this is not a coupled or general ODE solver.

Viewer packaging exposed destructive npm behavior through a shared dependency
symlink. Primary dependencies were restored from their lockfile and that
checkout is clean; packaging now refuses the symlink before installation,
proved red/green. Three moderate locked dependency advisories are reported,
not silently addressed with an unrelated forced upgrade.

An extra export/capture suite initially ran concurrently with the running
suite; both use `tests/_build`, causing deletion/render races. Those runs are
not clean passes. Their version-gate assertions also still expected v5;
the tests now expect v11, retaining pre-browser refusal and unchanged posed
capture behavior. Final producer validation runs all selected suites in one
process so they do not share a scratch directory across concurrent runs.

The framework schema still carries old one-commit and framework-local web
build guidance. The shop's controlling two-commit protocol and independent
viewer ownership were followed instead; no framework-local agent workflow
was used. The archive skill's optional archive-instructions lookup is not
implemented by this schema; its supported status/spec checks and explicit
archive move were used, preserving `.openspec.yaml`.

## Initial integration boundary (before renewed pilot authority)

Framework primary advanced independently from e6a42c8 to
`b0fb36957ddb054463b2b4b3da7ac2689b4f4955` (documentation of the released
mechanics package). The framework-change integration rule requires a new
pilot decision for a changed base; no merge/rebase/cherry-pick was attempted.
The unrelated `docs/examples/v8-engine/` remains untracked and untouched.
This manually approved framework bench has no launcher manifest or ports;
later cleanup must account for that. Viewer primary and other worktrees,
shop's two untracked documents and Curta's two untracked media files remain.

Native station-8's prior 23,556 checks/zero failures and the unadopted collar
trial's three passes/8,209.898 s remain independent evidence. No complete
native-machine geometry acceptance, station-10/11 clearance acceptance or
new default operating model is claimed. Both fixes are isolated; integration,
project adoption, pushes and publication have not occurred.

## Authorized integration reconciliation

The pilot subsequently said “go” in direct response to the request to rebase
onto b0fb369 and integrate both fixes, preserving the unrelated untracked
directory. This authorizes the local rebase and fast-forward integrations;
it does not authorize pushing or publication. The clean-primary exception is
limited to preserving `docs/examples/v8-engine/` in place: no incoming change
touches that path, and tracked primary files are clean.

The framework cycle was rebased onto
`b0fb36957ddb054463b2b4b3da7ac2689b4f4955` without conflicts. Planning commit
is now `4569c7e`; `git range-diff e6a42c8..7852480 b0fb369..5046f1e`
reports both patches unchanged. The only subsequent edit is this evidence
update in the completion commit, retaining the two-commit cycle.

Post-rebase combined producer validation: **790 passed, 797 subtests passed**,
58.91 s, three warnings and the same separately proven opt-in capture skip.
Viewer integration remains a fast-forward from c8da56e to the tested
684c583 completion. These checks authorize the pending local fast-forwards;
their resulting commit identities and project adoption checks belong in the
originating Curta record. Geometry boundaries above remain unchanged.
