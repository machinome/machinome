# Completion evidence

Framework base `6ff98062d47b9c70e9c6c3637944e02148fa1cfb`; frozen Curta project head `1f807fb` for the paired benchmark. The pilot's clean isolated worktree exception preserved primary's pre-existing untracked `docs/examples/v8-engine/` unchanged.

Red-first test: repeated evaluation of one shared graph invoked `postorder` three times (two successful input sets and one missing-input error); the requirement is one traversal with fresh numeric results on each call. After the instance-local order cache, two focused tests pass, including changing inputs, missing input, operation order, degree sine and graph collection.

The mesh-free OperatingCurta operation is `Sim(OperatingCurta(), dt=.1)`, `digit_1=3`, then one 18° crank move over 0.1 simulated seconds. Before and after on the same project head, process CPU for the crank tick was 6.719 and 5.829 seconds respectively (13.3% reduction). The full state has 213 entries and the exact same SHA-256 digest `ee8d6f04adcab85a32bf0920951aa375412860c8bd259e52079a06c7540105e2`. CPU time avoids distortions from concurrent CAD work; the result is one bounded tick, not a claim about all operating paths.

Expression, expression-sharing, corpus, running source-timing, carry, stop, read and selection suites: 275 tests and 134 subtests passed. No search samples, tolerances, law evaluation order, graph sharing, document format or geometry were changed. No ADR is needed: this is a local cache on an existing immutable graph value and adds no public interface or architectural dependency.
