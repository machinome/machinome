## 1. Contract and red evidence

- [x] 1.1 Add focused failing tests for stable private operation dispatch, exact numeric bits, current standing values, errors and graph release.
- [x] 1.2 Record baseline frozen-Curta ordinary-turn/tick CPU and ordered sample/bank identity on framework `3afb3f7`.

## 2. Implementation

- [x] 2.1 Prepare per-path immutable numeric instructions only after a successful eager bind, preserving all original node/operand order and call semantics.
- [x] 2.2 Evaluate later samples through those instructions with fresh sample slots and current standing values; keep the existing visit count.

## 3. Validation and completion

- [x] 3.1 Run focused and broader framework regressions, strict OpenSpec validation, and exact frozen-Curta before/after sample/bank parity.
- [x] 3.2 Require a material real-Curta CPU gain; record the decision and sync/archive the accepted change, or report a no-gain result without integration.
