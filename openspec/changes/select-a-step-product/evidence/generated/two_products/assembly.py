# Solid Node - generated by `solid import-step`

"""Assembly scaffolded from 'two_products.step' by `solid import-step`.

Generated once; edit freely -- running the command again
refuses to overwrite this file (see docs/cli.rst). This is
the machine at rest: no driver, no simulate() -- add motion
once the layout looks right.
"""

from solid_node.node import AssemblyNode

from .parts import ColouredPart, PlainPart


class M(AssemblyNode):

    colouredpart = ColouredPart()
    plainpart = PlainPart()

    def render(self):
        # ColouredPart (two_products.step)
        # colouredpart is placed at the identity
        # PlainPart (two_products.step)
        # plainpart is placed at the identity
