## 1. Pin the originating failure and red contracts

- [ ] 1.1 Retain the small public Driver → Prismatic reproducer, static and read-dependent Bound variants, the Curta oracle input/report SHA and framework/viewer base heads in cycle evidence; add red assertions for exact target bits, `completed` status and no false stop.
- [ ] 1.2 Add red zero- and positive-duration scenarios for both rounding directions, signed zero, a true beyond-bound stop, unchanged relative move, active snapshot/restore, legacy snapshot form, malformed target finite/type atomic refusal, and independent-run replay. Capture first-error/atomicity baselines before modifying runtime code.

## 2. Preserve an absolute endpoint through the running path

- [ ] 2.1 Retain the once-converted native `to` endpoint on `Command` separately from the unchanged timed ramp/delta so interior arithmetic remains byte-identical; round-trip an optional finite numeric target through active snapshots without changing legacy command records, `by`, rates or public program documents.
- [ ] 2.2 Thread a bit-different terminal endpoint through tick-local source Motion and ordered downstream propagation before `_reached`; preserve native delta and all interior samples, stop segments, absolute landings, first-error order and no-correction hot path. If any exercised relation kind cannot be handled without approximating its path, return the concrete red case for plan review rather than commit a partial correction.
- [ ] 2.3 Make terminal commit and handle reporting exact for successful `to`, while a genuinely beyond-bound request still blocks at the same physical stop. Confirm no tolerance, post-hoc status relabel or project-specific exception entered the implementation.

## 3. Verify the real caller and the paired runtime

- [ ] 3.1 Run focused command, static/dynamic Bound, trajectory, Play/Follow/self-read, snapshot and stop suites, then the full framework suite; compare unaffected `by`/rate and older program-document bytes with the pinned base.
- [ ] 3.2 Rerun the current Curta `CompiledReverserTrial` direct-withdrawal Python oracle with all 214 bank bits, statuses, stops and replay; both earlier `to=-4.9425` and final `to=3.9075` may correct their old bank bits to their exact targets, while earlier contact/stop semantics remain valid. Do not compare changed target bits to the erroneous old-bank golden, edit Curta source or adopt a geometry fit.
- [ ] 3.3 Coordinate a separately owned viewer correction using the same tiny fixture and actual Curta command sequence; require exact cross-runtime bank/status/stop parity before claiming browser readiness, without editing viewer source from this framework cycle.

## 4. Complete the framework record

- [ ] 4.1 Decide from implemented evidence whether an ADR or architecture amendment is warranted; keep this an internal conformance fix if no new architecture decision is made. Record tests, source pins, limits and baseline comparison.
- [ ] 4.2 Strict-validate and sync the simulation delta spec, archive the change, and create a clean two-commit framework branch after root review. Integrate locally only after authorized paired validation and base recheck; do not push or publish.
