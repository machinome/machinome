## 1. Preserve the originating evidence and prove the missing operation red

- [ ] 1.1 Copy the capped actual-Curta SAT materialization diagnostic from its pinned SHA into this change's durable evidence using a patch; record its 16/64/128/256-pair counts without attempting the unbounded 633,552-pair graph.
- [ ] 1.2 Add focused red tests for an independently supplied polygon profile, a symbolic `profile_overlap` call in a running Bound, compact program publication, and a different table changing snapshot identity; capture the pre-change failures. Include exact repeated-vertex/zero-length-edge/pentagram rejection and a red documentation/import check for the new public names.
- [ ] 1.3 Record the native-only cover's positive standalone-drum-mesh remainder and the still-unaccepted native-plus-mesh union as project obligations, not green contact-certification results.

## 2. Implement the finite pointwise primitive

- [ ] 2.1 Implement immutable `ConvexProfile(polygons)` validation with exact binary area/orientation and nonadjacent-edge intersection, accepting collinear vertices and independent loops while refusing nonfinite, repeated vertices, zero-length edges, self-crossing, degenerate and concave input without repair.
- [ ] 2.2 Implement numeric rigid-XY `profile_overlap` with finite-input/finite-intermediate guards, the specified binary64 angle conversion and ordered transform/AABB/SAT arithmetic, strict separation and inclusive touching; prepare/validate both entire transformed profiles before pair AABB early return. Test clear, touch, rotated/translated contact, signed zero, huge finite binary64 angles without an arbitrary cutoff, unrepresentable numeric input conversion, transformed-edge collapse including a far-away other profile, projection overflow, malformed and nonfinite cases. Direct Python evaluation and v13 fixture expected results MUST use the same ordered arithmetic, including huge-angle cases.
- [ ] 2.3 Add one graph operation with immutable profile leaves for symbolic placements, preserving profile ownership through enclosing arithmetic and no mutable process-global registry; prove graph size does not scale with polygon-pair count.
- [ ] 2.4 Red-test repeated complete placement and pair computation on the actual installed Curta profile sizes, then add a private per-running-integration successful-value reuse with 1,024 pair/256 placement entry caps and exact finite builtin IEEE-bit keys. Test signed zero, distinct profile objects/tables, custom and nonfinite operands, failed preparation/evaluation without publication, LRU eviction, reset/replay, two runs, ordered first errors and unchanged Bound samples; no direct-call cache outside an integration attempt.

## 3. Integrate only with existing running Bound and the versioned program

- [ ] 3.1 Admit the profile operation only in compiled running Bounds and their numeric/path evaluators, with conservative non-affine classification and original eager first-error order; refuse unsupported **symbolic** law/pose/clocked contexts by name while preserving standalone numeric and numeric bind-time range use. Update the existing Bound-specific symbolic-vocabulary rule without widening general math/SCAD calls.
- [ ] 3.2 Publish a deterministic, content-deduplicated `program.profiles` table and literal-index `profileOverlap(...)` scalar calls, preserving binding aliases and checking references; include complete content in program/snapshot identity and select v13 only when used.
- [ ] 3.3 Prove no-contact models retain their previous version, document bytes, program identity, Bound samples, bank, stops and refusal behavior; test two independent runs with distinct profile tables and reset/restore lifecycle.
- [ ] 3.4 Generate a compact producer fixture pinning placement, strict clearance, shared-endpoint touch, near-contact, a Bound stop, relief and replay for the separate viewer consumer cycle; verify old viewers reject v13.

## 4. Measure and verify against the Curta and paired viewer

- [ ] 4.1 Evaluate the actual exploratory Curta native and native-plus-mesh profiles at the eight pinned placed probes, compare with the independent native contact records, and report both agreement and the incomplete installed-print/axial scope explicitly.
- [ ] 4.2 Measure Python and paired viewer point-evaluation and bounded operating-request cost on the accepted project profile data; if the final installed cover grows beyond the current fixture or a pair becomes impractical, return to planning rather than truncate or change sampling.
- [ ] 4.2a Compare the pinned one-tick installed-trial baseline and candidate with full ordered contact-input/result bit trace, all bank/stop bits, predicate/placement call counts and CPU time. Report only measured savings; coordinate the paired viewer's independent tick-local cache and do not claim full operating performance from this one tick.
- [ ] 4.3 Require the separately owned viewer change to load and evaluate the same v13 fixture in its own program/table scope, including binding aliases, independent mounts and actual Curta contact cases; do not claim portable operation until the paired gate passes.
- [ ] 4.4 Run relevant framework suites, strict OpenSpec validation and a bounded current-project local trial of the existing axial Bound using the new flag for stop, relief and snapshot/replay; record source pins, exploratory cover scope and unresolved installed-print/axial/production obligations honestly. Full project production adoption is **not** a capability/archive gate.

## 5. Document the public current-source capability

- [ ] 5.1 Add tested public docstrings at `machinome/simulation/profile.py`, covering both `ConvexProfile` and `profile_overlap`, their import path, inclusive touch, refusal conditions and pointwise/sampling limit. Test `help()`/imports directly. Keep released teaching pages (`docs/reference/api.rst`, `docs/concepts/running.rst`) on their 0.7 contract until an explicit release pass; do not add a change-specific page or claim a continuous collision certificate.
- [ ] 5.2 Put the current-source capability and v13/paired-viewer requirement in `docs/project/status.rst` and an `Unreleased` entry in `docs/project/changelog.rst` (and contributor `HISTORY.rst` if kept current), leaving released 0.7 facts in `README.rst`, `docs/conf.py`, the release note and substitutions unchanged. Do not describe an unreleased capability as published.
- [ ] 5.3 Extend `tests/test_docs_structure.py` or focused API/documentation tests red-first to pin both public imports/docstrings and released/current-source separation. Run the strict Sphinx HTML build and relevant docs tests; inspect the changed status/changelog pages. If `README.rst`, package metadata or manifest changes become necessary, also run `python scripts/check-dist` and record why.

## 6. Complete the framework cycle if all gates pass

- [ ] 6.1 Sync the accepted baseline specs, including `user-documentation`, determine whether an ADR/architecture amendment is warranted from the implemented design, and archive the completed change using the supported workflows.
- [ ] 6.2 Verify a clean two-commit framework branch with complete evidence; request/recheck authorized local integration separately, with no push, release, viewer-source edit or Curta-source edit from this worktree.
