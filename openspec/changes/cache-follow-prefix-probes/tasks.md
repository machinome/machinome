## 1. Establish the duplicate replay and red contract

- [ ] 1.1 Add a focused paired Follow-Bound fixture proving identical prefix probes currently rerun, and assert unchanged first errors, bit-distinct fractions, different edge sequences and later stretches.
- [ ] 1.2 Run the focused test red against the recorded base and preserve the failure output.

## 2. Reuse only proven successful prefixes

- [ ] 2.1 Add a `_reached`-local successful prefix snapshot for the exact deterministic law-to-Follow subprogram and IEEE fraction, leaving Bound level/search code in place.
- [ ] 2.2 Cover one-sided cut neighbors, source/Bound failures, restore/retry, different programs and two ticks with green focused tests.

## 3. Verify origin and complete the cycle

- [ ] 3.1 Benchmark identical project source and five 0.1-second production Curta crank ticks before/after with wall and CPU times, and verify full bank/status/stop/replay and ordered sample parity where applicable.
- [ ] 3.2 Run relevant framework suites and supported OpenSpec validation; classify any unrelated environmental failures honestly.
- [ ] 3.3 Sync and archive the change, assess ADR need, and create the complete implementation commit; coordinate concurrent-main reconciliation and local fast-forward integration only after gates.
