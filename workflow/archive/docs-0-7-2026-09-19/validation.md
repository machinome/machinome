# 0.7 documentation refresh — validation record

This is maintainer release-preparation work, not a release announcement.

## 2026-09-19: Machinome release refresh

The pilot requested that the existing `docs-0-7` worktree be brought up to
the evolved framework, renamed organisation and **Source code for machines**
narrative. This continues the directly approved documentation work recorded
below. It changes no framework behaviour or architectural decision, and
opens no new OpenSpec cycle.

### Repository state and scope

- Previous documentation commit: `cb884f5705a2a0fa206d1c60266a63d42cccc068`.
- Current framework incorporated: `e30b14af2ded68fbcf9c232a4c4d4d9b852cbd5d`
  from `main`. The original branch base was
  `2bdc50b37be920e79202d1c9e9c5700e43f525e0`.
- Worktree: `machinome-framework/WTs/docs-0-7`, branch `docs-0-7`.
  Git still registered its former `libresolid-studio/solid-node` path;
  `git worktree repair` repaired this exact worktree's registration.
- Current `main` was merged into the documentation branch, retaining the
  previous documentation commit and the framework's intervening history.
  No primary checkout, project source or viewer source was edited.
- The README, landing and why pages now describe a machine's source as
  binding its pieces, shared dimensions, motion, operation and tests.
  The original gradual tutorial and three sibling external examples remain.
- The migration map covers packages, imports, commands, configuration,
  viewer integration and organisation URLs. It explicitly preserves the
  `SOLID_BUILD_DIR` and `SOLID_TEST_*` names that the implementation still
  reads; only former `SOLID_NODE_*` settings take the `MACHINOME_*` prefix.
- Release/status, simulation, viewer and embedding guidance now distinguish
  posed, running and clocked machines, stored state, elapsed clocks,
  instructions and stops. Viewer guidance uses the implemented API 20 and
  schemas 1–8, with separate `run()` and `machine()` handles.
- Markings and expression-graph guides are reachable from navigation.
  New release notes are in `release-0.7.rst`; historical release wording
  remains historical, and `development-0.7.rst` preserves the current
  engineering record including intermediate spellings.
- Both documentation builders export all three examples with the Machinome
  CLI and renamed remotes. Read the Docs uses Node 22 like Actions.
  Package description and Context7 guidance use the new narrative and
  current contracts. No new ADR or spec synchronisation is required.

### Validated source combination

| Component | Commit |
| --- | --- |
| Framework product content | `e30b14af2ded68fbcf9c232a4c4d4d9b852cbd5d` |
| Machinome Viewer | `8495d74be1329c2c3170a03c66aab5f02d5d7442` |
| V8 example | `fdf624b9d07831bda6baa690dfa7d6bf3aef400c` |
| Metamaquina 2 example | `c916f9b5f09ba27faaab9225fff0cdf6342fc00c` |
| Clock 01 / 3DPrintedClocks | `b089545bfcb8bbb507e480c6381010cd8281fb89` |

The example commits were fetched from their independent project repositories
in the workspace into the existing documentation submodules. Only gitlinks
were updated in the framework; no external project source was changed.
Existing untracked exports were preserved and regenerated by the exporter.

### Verification

- Initial strict Sphinx build failed on old-format example manifests,
  stale autodoc members, an unlinked markings guide and unresolvable source-file
  references. Corrected the documentation and regenerated the real examples.
- `python -m sphinx -E -b html -W --keep-going docs docs/_build/html`:
  **passes without warnings**, including all three external machine exports.
- `python -m pytest -q tests/test_docs_exports.py tests/test_sphinx_ext.py
  tests/test_declarative_nodes.py tests/test_time_base.py tests/test_joints.py
  tests/test_clocked_sim.py tests/test_clocked_time.py
  tests/test_machinome_identity.py`:
  **435 passed, 374 subtests passed**. Five warnings are existing build123d
  deprecations and the intentionally exercised legacy render-time warning.
- The first identity test run detected the old worktree's ignored
  `solid_node/` bytecode cache. Inspection found only `.pyc` files; that exact
  directory was moved recoverably to `docs/_build/pre-rename-python-cache`.
  The unchanged identity check then passed.
- README parsed with docutils at warning-as-error severity.
- `openspec validate --specs --strict --no-interactive`:
  **34 passed, 0 failed**. No baseline spec was changed.
- `machinome viewer` reports package **0.2.0**, API **20**, and document
  versions **1–8**, matching the guide.
- Headless Chromium checked the landing page, migration table and all three
  example pages over local HTTP. Each example mounted exactly one model with
  API 20; no page JavaScript error or failed HTTP response occurred.
  Screenshots of the landing page, migration guide, V8, Metamaquina 2 and
  Clock 01 were inspected. The final heading/table adjustments were also
  checked on the landing, migration and embedding pages.
- Generated HTML, CAD exports, local browser-check script and screenshots
  stay in ignored build directories, not the commit.
  `git diff --cached --check main` passes for the documentation delta;
  historical whitespace in archived evidence arriving from `main` is unchanged.

This is focused documentation and API-contract verification, not a full
framework release test run or a certification of the examples' complete
mechanical test suites. No CAD geometry or runtime behaviour was modified;
the earlier Orbit docstring correction remains the only source-code delta
against the current framework.

### Remaining publication prerequisites

The local manual is validated. A fresh public build cannot yet reproduce it:

1. Publish the three exact example revisions above to their configured
   `machinome` GitHub repositories. At validation time, GitHub's public commit
   API returned HTTP 422 (no commit found) for each revision. The pinned
   source links deliberately identify the source used by the exports.
2. Publish the matching viewer source revision. Public viewer `main` was
   `a45b027fea71af9cfe0dd771d4037940580114dd`, whose distribution still calls
   itself `solid-node-viewer`; the two local rename commits culminate in
   `8495d74`. Both documentation builders require the renamed package.
3. Integrate this documentation branch into framework `main` under the
   pilot's release authority, then run the full release checks. Package
   publication, tagging and changing release-preparation labels to a dated
   release remain separate release actions.

No push, tag, package publication, PR, main-branch integration, branch deletion
or worktree removal was performed. The primary framework remains at `e30b14a`.

## Original 2026-09-11 validation (historical)

The following records the earlier Solid Node preview, before the rename and
the running/clocked additions above. Its versions and source pins describe
that validation only.

## Scope and provenance

- Framework base: `2bdc50b37be920e79202d1c9e9c5700e43f525e0`.
- Isolated branch/worktree: `docs-0-7`.
- The pilot approved a direct documentation refresh on that committed base,
  leaving the primary checkout's existing workflow edits alone, and then
  requested an amendment to restore the original teaching progression.
- The tutorial builds upon the framework's existing disc, pointer and pin
  example: model parts, assemble them, animate the pointer, test its fit,
  then introduce declarative parameters, joints and drivers.
- The rejected Clock 01 tutorial implementation, geometry wrapper, tests,
  package configuration, LICENSE and NOTICE have been removed. No
  3DPrintedClocks design source is copied or reimplemented in the tutorial.
- V8, Metamaquina 2 and Clock 01 are three sibling external examples, each
  with a source link and a live model. Their source remains in independent
  repositories tracked through Git submodule references, not framework
  source files. Viewer exports are generated during the documentation build.
- Clock 01 credits Luke Wallin's original
  [3DPrintedClocks](https://github.com/MrBunsy/3DPrintedClocks) project.
  Its separate example page links the external licence and briefly explains
  the source-distribution obligation.
- The framework and its own tutorial source remain Apache-2.0. The
  independent viewer is AGPL-3.0-only; external designs retain their own
  licences.

The refresh covers the introduction, learning path, assembly/motion/time
tutorials, migration guide, user-facing release notes, examples, reference
corrections and both documentation builders. The former unreleased engineering
changelog is preserved in `development-0.7.rst`, explicitly labelled as
intermediate development history. The only framework source edit corrects
reStructuredText in the Orbit docstring; no runtime behaviour changes.

## External source revisions

The documentation uses existing committed v0.7 migrations from the independent
project repositories. Their older public revisions still import ports from
the old module and cannot export with this framework base.

| Example | Pinned source commit |
| --- | --- |
| V8 engine | `a25b0749a34a1594dbcbef1aa38acc4835c2ff6e` |
| Metamaquina 2 | `b77d67cfdaf36a3ec110ba065af72a39f75931cb` |
| Clock 01 | `0e496374716f03f1ab8026179088e7b14a9cb8fc` |

These commits were fetched locally from the workspace's independent project
repositories for validation. They were not pushed by this change and were
not on the configured public branches at validation time. Making these exact
revisions available from the configured submodule remotes is a prerequisite
for a fresh public checkout, CI and Read the Docs. No project source or
pre-existing project edits were changed or committed by this work.

## Validation

Run from the framework worktree with the workspace environment and this
worktree on PYTHONPATH:

- `sphinx-build -E -b html -W --keep-going docs docs/_build/html`:
  clean full build, including all three live external examples and the
  original small tutorial embeds.
- `pytest -q tests/test_docs_exports.py tests/test_sphinx_ext.py
  tests/test_time_base.py tests/test_joints.py`:
  **192 passed, 278 subtests passed**. One expected FutureWarning exercises
  the legacy render-time compatibility path.
- New documentation regression checks failed before the correction, then
  passed after restoring the sibling example pages and framework-owned
  tutorial progression. Existing checks verify export coverage in both
  CI and Read the Docs and one external model per example page.
- README parsed independently with docutils, warnings treated as errors.
- `git diff --check`: clean.

Each external model exported successfully from its own submodule directory:

- V8: `solid export -o docs/_exports/v8-engine`.
- Metamaquina 2: `solid export -o docs/_exports/metamaquina2`.
- Clock 01: `solid export -o docs/_exports/clock01 wall_clock_01`.

These were real CAD builds, not mock geometry. Browser inspection of the
three example pages found a rendered model in each, with no JavaScript
errors; screenshots of all three were inspected. The restored assembly
lesson also rendered its three small embedded models. Generated exports
and screenshots are not committed.

The restored pin-fit exercise was executed through `solid test` in a
temporary project outside the repository:

1. Deliberately oversized pin (radius 3.1): both fit assertions failed.
2. Clearance at rest (radius 2.99): both passed.
3. The same coarse geometry swept over 16 instants: pointer fit failed.
4. Fine tessellation (`fn = 256`): both 16-instant sweeps passed.

This checks the lesson's red/green progression, including the difference
between a fit at rest and clearance throughout motion. The new joint lesson
was also evaluated numerically: a quarter timeline turn and a manual
driver setting each produced -90 degrees; a 0.5 follower relation produced
-90 degrees from a -180-degree input.

## Limits and release prerequisites

Exporting and inspecting an external example is not a complete mechanical
test suite, a dynamics validation or a manufacturing certification. This
documentation change did not run or certify every external model's full
geometric test suite.

The installed viewer reports API 7, package 0.1.0, supporting schemas 1–4.
That package is not yet published. Public CI and Read the Docs require
the matching viewer source and all three pinned example revisions to be
available remotely; viewer source installation remains explicit in their
configuration. Release installation commands are labelled as future commands
until 0.7 and the viewer are published.

No package version, release tag, remote branch, PR or publication was changed.
Integration into the primary branch remains the pilot's decision.
